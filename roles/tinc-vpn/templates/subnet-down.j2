#!/bin/bash

{% if ec2_tag_classifier == 'spoke' %}
[ -z  "${NODE##*hub*}" ] && exit 0
{% endif %}


{% if ec2_tag_classifier == 'hub' %}
[ -z  "${NODE##*spoke*}" ] && exit 0
{% endif %

route del -net ${SUBNET} dev tun0